function Frame () {
  this.score = 0;
  this.rollInFrame = 1;
  this.isStrike = false;
  this.frameScore = 0;
}

Frame.prototype.ballRoll = function(pins) {
  this.ballRoll(8);
  this.framescore += pins;


  // frame.ballRoll(8)
  // if (this.rollInFrame) === 1 && pins === 10) {
  //   this.isStrike = true;
  //   this
  // }

// Frame.prototype.

}
